# OS cruft
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Node — dist/ is generated by `npm run build` but stays tracked in git,
# since that's the file HACS actually serves. Only these are ignored.
node_modules/
npm-debug.log*
yarn-error.log*

# Env / local config
.env
.env.local