# OS cruft
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Node (only relevant if you add a build step later — dist/ itself stays tracked)
node_modules/
npm-debug.log*
yarn-error.log*

# Env / local config
.env
.env.local